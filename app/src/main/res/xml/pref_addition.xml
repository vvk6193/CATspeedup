<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <EditTextPreference
        android:key="numCountAddition"
        android:title="@string/count_number"
        android:defaultValue="@string/count_default_number"
        android:selectAllOnFocus="true"
        android:inputType="number"
        android:singleLine="true"
        android:maxLines="1" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="number_of_digit_addition"
        android:title="@string/pref_title_addition_digits"
        android:defaultValue="-1"
        android:entries="@array/pref_addition_list_titles"
        android:entryValues="@array/pref_addition_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="addition_table_size"
        android:title="@string/pref_title_addition_table_size"
        android:defaultValue="-1"
        android:entries="@array/pref_addition_table_list_titles"
        android:entryValues="@array/pref_addition_table_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
